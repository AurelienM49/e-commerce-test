<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <meta
      name="author"
      content="Mark Otto, Jacob Thornton, and Bootstrap contributors"
    />
    <meta name="generator" content="Jekyll v4.1.1" />
    <title>{% block title %} EASY {% endblock %}</title>
    <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet" />
    <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet" />

    <script
      src="https://kit.fontawesome.com/35b0a46636.js"
      crossorigin="anonymous"
    ></script>

    <!-- Bootstrap core CSS -->
    {#
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    #} {#
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
    #}

    <!-- Custom styles for this template -->
    <link href="{{ asset('assets/css/carousel.css') }}" rel="stylesheet" />
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-lg fixed-top navbar-light navbar-custom">
        <a class="navbar-brand title-nav" href="{{ path('home') }}"
          >EASY <i class="fa-brands fa-cotton-bureau fa-shake"></i
        ></a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link item-custom" href="{{ path('products') }}"
                >Produits</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link item-custom" href="#">Notre histoire</a>
            </li>
            <li class="nav-item">
              <a class="nav-link item-custom" href="#">Contact</a>
            </li>
            <li class="nav-item dropdown">
              <a
                class="nav-link dropdown-toggle item-custom"
                href="#"
                id="navbarDropdownMenuLink"
                data-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="false"
              >
                {% if app.user %} Login {% else %} Compte {% endif %}
              </a>
              <div
                class="dropdown-menu"
                aria-labelledby="navbarDropdownMenuLink"
              >
                <div class="nav-item-custom">
                  {% if app.user %}
                  <a
                    class="dropdown-item item-custom"
                    href="{{ path('account') }}"
                    >Mon compte <br />
                    (<small>{{ app.user.firstname }}</small
                    >)
                  </a>
                  <a
                    class="dropdown-item item-custom"
                    href="{{ path('logout') }}"
                    >Déconnexion</a
                  >
                  {% else %}
                  <a
                    class="dropdown-item item-custom"
                    href="{{ path('login') }}"
                    >Connexion</a
                  >
                  <a
                    class="dropdown-item item-custom"
                    href="{{ path('register') }}"
                    >Inscription</a
                  >
                  {% endif %}
                </div>
              </div>
            </li>
          </ul>
        </div>
      </nav>
    </header>

    <main role="main">
      {% block carroussel %} {% endblock %}

      <div class="container marketing body-custom">
        {% block body %} {% endblock %}
      </div>
    </main>
    {% block footer %} {% endblock %}

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="{{ asset('assets/js/bootstrap.bundle.js') }}"></script>
  </body>
</html>
